## Commands For This Task
- `:args <glob_or_paths>` - set argument file list.
- `:next` / `:prev` - move through argument files.
- `:argdo %s/<lookup>/<replace>/g | update` - run replacement + save on all arg files.
- `:%s/<lookup>/<replace>/g` - replace in current file.
