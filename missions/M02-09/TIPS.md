## Commands For This Task
- `:vimgrep /<pattern>/ **/*.py` - search pattern across multiple files.
- `:copen` - open quickfix list.
- `:cnext` / `:cprev` - move through quickfix entries.
- `:cdo %s/<lookup>/<replace>/g | update` - run replacement + save on every quickfix match.
